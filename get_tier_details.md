# get_tier_details

Returns full details for a single VIP tier by ID — includes reward configurations (benefits), locales, icon, and pointing configuration.

## Parameter

`id` — the tier/level ID. Get from `get_tiers`.

## Notes

- For browsing all tiers (slim payload) use `get_tiers`.
- Read-only — tier edit is dashboard-only.
